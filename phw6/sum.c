#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<mpi.h>

int main(int a, char **argv) {

	FILE *fp;
	
	
	
	fp = fopen("numbers.txt");
	if(fp == NULL) {
		printf("file open error\n");
	}
	while (fgets(a, )) {

	}


}
